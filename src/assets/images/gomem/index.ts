import { commonImages } from "./common";
import { epicImages } from "./epic";
import { hiddenImages } from "./hidden";
import { legendImages } from "./legend";
import { rareImages } from "./rare";

export const gomemList = {
  common: commonImages,
  rare: rareImages,
  epic: epicImages,
  legend: legendImages,
  hidden: hiddenImages
};
