import { StartButton, StyledHomeTemplate } from "./homeTemplate.style";

export const HomeTemplate = (): JSX.Element => {
  return (
    <StyledHomeTemplate>
      <StartButton />
    </StyledHomeTemplate>
  );
};
